{% extends 'admin.html.twig' %}

{% block title %}Administration-Actualité{% endblock %}

{% block header %}{% include "admin/header.html.twig" %}{% endblock %}

{% block body %}
  <table class="table">

  <thead class="thead-dark">
    <tr>
      <th scope="col">Id</th>
      <th scope="col">titre</th>
      <th scope="col">slug</th>
      <th scope="col">description</th>
      <th scope="col">texte</th>
      <th scope="col">image</th>
      <th scope="col">date</th>
      <th scope="col"><a href="{{path("admin_add_actu")}}"><button type="button" class="btn btn-success" >add</button></a></th>
    </tr>
  </thead>
  <tbody>
  {% for actu in Actus %}
    <tr class="borderTop">
      <th scope="row">{{actu.id}}</th>
      <td>{{actu.title}}</td>
      <td>{{actu.slug}}</td>
      <td>{{actu.description}}</td>
      <td>{{actu.text}}</td>
      <td>{{actu.image}} <br> <img src="\uploads\{{actu.image}}" class="img-fluid img-show"></td>
      <td>{{actu.createdAt|date("d/m/Y-H:i:s")}}</td>
      <td><a href="<?php echo 'index.php?id='. $actu['id'].'&action=modify&type=actu'; ?>"><button type="button" class="btn btn-warning" >modif</button></a><a href="<?php echo 'index.php?id='. $actu['id'].'&action=delete&type=actu'; ?>"><button type="button" class="btn btn-danger" >suppr</button></a></td>
    </tr>
  {% endfor %}
  </tbody>
</table>

{% endblock %}